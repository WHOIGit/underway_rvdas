# sbe48.yaml
sensors:
  sbe48:
    fields:
      Temperature:
        pattern: '# (\d+\.\d+),'
        transform: float

    # Optional: converters for additional fields
    converters:
      TemperatureCelsius:
        source: Temperature
        algorithm:
          type: round
          precision: 2